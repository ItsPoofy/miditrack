# 任意の音源チップで鳴らす機能 — 立案

編集後のMIDIトラックを、元ファイルのチップとは無関係に、ユーザーが選んだ音源チップ
（YM2612、YM2151、SN76489 など）のエミュレーションで鳴らせるようにする機能の設計案。

対象読者は `miditrack` の保守者。実装前の合意形成用で、確定した契約は各パッケージの
`CLAUDE.md` へ移す。

---

## 1. 目的

- どの入力（`.mid` 単体を含む）でも、トラックごとに任意のチップ音源で音を出せる。
- **編集結果が反映される**チップ音を得る。現在の「原曲の音源」は元ファイルの再生を
  チャンネル単位でミュートしているだけなので、音色変更・移調以外のノート編集は
  チップ音側に一切反映されない。本機能はここを根本的に変える。
- 既存の SoundFont / 原曲の音源という選択肢と同列に並ぶ、第3の音源として統合する。

### 非目標（初期リリース）

- リアルタイム再生（ソフトシンセ化）。`miditrack/CLAUDE.md` の「Rendering is offline;
  there is no live softsynth」を維持する。
- PCM/ADPCM系チップ（SegaPCM、C140、MSM6295、YM2608のリズム音源）。サンプルROMが
  必要で、データの入手・ライセンスが別問題になる。
- 原曲の音色の自動移植（元のFMパッチを吸い出して別チップへ載せ替える）。将来の拡張。

---

## 2. 現状の整理

### 2.1 いまのトラック音源

`WebSession.track_sources` が取りうる値は `"soundfont"` と `"game"` の2つだけ
（`web_session.py:244`）。`"game"` の意味はフォーマットで異なる。

| 入力 | `"game"` の実体 | 実装 |
|---|---|---|
| SPC | spc2midi が生成したゲーム由来SoundFont | FluidSynthのバンク切替（MIDI分割に残る） |
| VGM | libvgm で元VGMを再生し、選択チャンネル以外をミュート | `libvgm.py` → `vgm2midi_stems --selection` |
| NSF | nsf2midi 自身のNES APUエミュレーション | `nsf_chip.py` → `nsf2midi --chip-render` |

VGM/NSF の `"game"` トラックは `_plan_render_jobs()`（`web_routes.py:620`）で
FluidSynth側のMIDIから除外され、実機WAVステムとして別レンダリングされ、
`mix.py` の ffmpeg `amix` で合成される。速度・ピッチはステム側を `rubberband.py`
で追従させている。

### 2.2 この構造から言えること

- **ステムを1本増やして混ぜる経路は既にある。** 新しい音源は「WAVステムを1本作って
  `mix.py` に渡す」だけで再生・WAVダウンロード・バリエーション・トラック別書き出しに
  一通り乗る。
- **チップ音は必ず元ファイル由来**という前提が現在の設計に埋まっている。新音源は
  「編集後MIDI由来」なので、キャッシュ無効化の条件が `"game"` と異なる（後述 7.4）。
- **libvgm が既にビルド済みで手元にある。** `vgm2midi/native/CMakeLists.txt` は
  約50のデバイスファミリを明示的に有効化し、ライセンス監査済みの universal binary を
  `vgm2midi/native/bin/vgm2midi_stems` としてコミット済み。新しい依存を足さずに
  多数のチップが使える状態が既にある。

---

## 3. ユーザーから見た機能

1. トラック行の音源トグルが **SF / 原曲 / チップ** の3択になる（「原曲」は従来通り
   対応フォーマットのみ表示）。
2. 「チップ」を選んだトラックは、GM音色セレクタの代わりに **チップ**（YM2612 等）と
   **音色**（そのチップのプリセット）の2段セレクタになる。
3. ヘッダに「全トラックのチップをまとめて選ぶ」コントロールを置く（実際の用途の大半は
   「全部YM2612で鳴らしたい」なので、1トラックずつの操作は主導線にしない）。
4. 和音数超過・音域外・ドラム未対応などは、既存の `conversionWarnings` と同じ場所に
   日本語で出す。例:「YM2151は8音まで — トラック3の12ノートを省略しました」。
5. `.miditrack` プロジェクトにチップ選択が保存され、再開時に復元される。
6. おまけ: 全可聴トラックがチップ音源のとき、**VGMとして書き出す**ことができる
   （方式上、VGMが中間生成物になるため実質無料）。

---

## 4. 実現方式の比較

新音源に必要なのは「MIDIノート列 → チップのレジスタ書き込み列 → 音」。3案ある。

### 案A（採用）: MIDI → VGM コマンド列 → 既存 libvgm ヘルパ → WAV

MIDIから**VGMファイルを合成**し、既存の `vgm2midi_stems` にそのまま食わせる。

- **ネイティブ側の変更が Phase 1 では不要。** `vgm2midi_stems --selection INPUT OUT
  TOTAL_SAMPLES TYPE:0:0xFFFFFFFF:0xFFFFFFFF` は「そのデバイスの全チャンネルを
  サンプル数固定でレンダリング」そのもの。合成したVGMを渡せばWAVが出る。
- レジスタ知識が既に `vgm2midi/src/chips/*.ts` にある（復号方向）。新モジュールは
  その逆関数で、同じ定数・同じ `midi-math.ts` を共有できる。
- 中間生成物がVGMなので**検証が容易**。vgmplay等の外部プレイヤーで直接鳴らせるし、
  既存のVGM→MIDIデコーダを**テストのオラクル**として使える（10.1参照）。
- VGMのウェイトは44.1kHz単位なのでタイミングはサンプル正確。ステム長を他と厳密に
  揃えられる。
- 副産物としてVGM書き出し機能が手に入る。

### 案B: libvgm のデバイスAPIを直接叩く新ネイティブシンセ

`emu/SoundEmu.h` でデバイスを生成し、ブロック単位でレジスタを書きながらレンダリング。

- VGMという中間表現を挟まない分、制御レートの自由度は高い。
- ただしチップごとにC++を書くことになり、ビルド対象・署名対象・クロスビルドの
  面倒が増える。案Aで表現できないことが出てきた場合の**退避先**として残す。

### 案C: ブラウザ内のJS/WebAudio合成

リアルタイム試聴はできるが、DSPを自前で書くことになり音の正しさの担保が重い。
`miditrack/CLAUDE.md` の「フレームワーク/CDN依存を入れない」「オフラインレンダリング」
という契約にも反する。**不採用**。

**→ 案Aを採用。** 以降は案A前提。

---

## 5. 全体データフロー

```
編集後MIDI (applied_path)
  │  ※ 音色/音量/速度/移調は適用済み
  ├─ SF トラック ────────► FluidSynth (midi2wav.sh) ──► dry.wav ─┐
  │                                                              │
  ├─ 原曲トラック ───────► libvgm/nsf2midi（元ファイル再生）──► orig.wav ─┤
  │                                                              ├─ ffmpeg amix ─► 再生用WAV
  └─ チップトラック                                              │
        │ ① スコア抽出 (Python: midi.py/pianoroll.py 由来)       │
        ▼                                                        │
     score.json  ──② 合成 (Node: midi2vgm)──► synth.vgm          │
                                                │                │
                                                ③ 既存ネイティブヘルパ
                                                ▼                │
                                             chip.wav ───────────┘
```

①②③ が新規。③は既存バイナリの既存モードをそのまま使う。

---

## 6. 新規コンポーネント

### 6.1 スコアJSON（①の出力 = ②の入力契約）

TypeScript側にSMFパーサを新設しない。`midi.py` が「トラック解析・音量・速度・移調の
source of truth」という既存契約（`miditrack/CLAUDE.md`）を守り、MIDIの読み取りは
Python側1箇所に保つため、**中間表現はノートイベントのJSON**とする。
`pianoroll.py` が既にテンポマップ込みで per-track のノート
（start/duration/note/velocity + ピッチベンド軌跡）を抽出しているので、その抽出部を
上限なしで再利用できる形に切り出す。

```jsonc
{
  "version": 1,
  "sampleRate": 44100,
  "durationSeconds": 132.48,
  "tracks": [
    {
      "index": 3,
      "chip": "YM2612",          // チップID
      "voice": "opn2:bass-01",   // 音色ID
      "isPercussion": false,
      "volumePercent": 100,
      "pan": 64,
      "notes": [
        { "t": 1.250, "d": 0.240, "n": 45, "v": 96,
          "bend": [[0.000, 0.0], [0.120, 2.0]] }   // ノート内の相対時刻[s]と半音
      ]
    }
  ]
}
```

- 時刻は**秒の絶対値**。速度・移調は適用済みなので、合成側はテンポを知らなくてよい。
- 1トラック = 1音色（モノティンバー）。複数チャンネルを含むトラック（format-0 等）は
  既存の音色編集不可ルールと同じ理由で「チップ」を選べない。

### 6.2 合成コア `midi2vgm`（②）

置き場所: `vgm2midi/src/synth/`。`package.json` の `bin` に `midi2vgm` を追加する。
（別パッケージにするとチップテーブル・`midi-math.ts`・ネイティブヘルパ解決を
二重管理することになるため、同一パッケージ内に置く。`vgm2midi/CLAUDE.md` の
スコープ記述は「VGM↔MIDI双方向」へ更新する。）

```
midi2vgm --score score.json --out synth.vgm --report report.json
```

構成要素:

**(a) チップカタログ** — チップIDごとに、libvgm の DEV_ID、既定クロック、VGMヘッダの
クロック書き込み位置、チャンネル表（本数・種別: FM / 矩形 / 波形 / ノイズ）、
レジスタ書き込みに使うVGMコマンド番号を持つ。コマンド番号は
`vgm-chip-metadata.ts` の `COMMAND_CHIPS` を逆引きするだけで足りる。
ヘッダのクロックオフセット（SN76489 = 0x0C、YM2413 = 0x10、YM2612 = 0x2C、
YM2151 = 0x30 …）は現在 `vgm-parser.ts` にベタ書きなので、共有テーブルとして
`vgm-chip-metadata.ts` へ切り出し、パーサとライタで共有する。

**(b) ボイスアロケータ** — トラック群 → チップの物理チャンネルへの割り当て。
同時発音数を超えたらボイススティール（解放済み → 最古 → 最小音量の順）。
ドラムトラック（MIDI ch10）はノイズチャンネルを持つチップならそこへ、
持たないチップなら省略して警告。決定的な動作にして、同じ入力からは必ず同じ
VGMが出る（ゴールデンテストのため）。

**(c) 音程変換** — 既存の復号関数の**逆関数**として実装する。例:

- SN76489: `midi-math.ts` の `psgRegisterToFrequency()` の逆 →
  `period = clock / (divisor * f)`、10bitクリップ。
- YM2612/YM2203/YM2608(FM): `ym2612FrequencyToHz()` の逆 →
  `fnum = f * 144 * 2^(20-block) / clock`、fnum が 0x400–0x7FF に収まる block を選ぶ。
- YM2151: KC（キーコード）+ KF（6bitの端数）。MIDIノート番号からほぼ直接引ける。
- YM2413/OPL: サンプルレート `clock/72` 基準の fnum/block。
- GB DMG: `period = 2048 - 131072/f`。

各チップに音域クリップと、丸め誤差（セント）の上限を単体テストで固定する。

**(d) 音量・パン** — ベロシティ × CC7 × トラック音量 を dB で合成し、チップの
減衰レジスタへ写像する。PSG系は4bit（SN76489は2dB/step、OPLLは3dB/step）、
FM系は**キャリアオペレータのTL**（0.75dB/step）。どのオペレータがキャリアかは
アルゴリズム依存なので、音色データにキャリアマスクを持たせる。
パンはCC10を3値（L/C/R）へ量子化（YM2612/YM2151のLRビット、SN76489はGGステレオ）。

**(e) 制御レート** — ノートオン/オフは**サンプル正確な位置**に置く。ピッチベンド・
ビブラート・エクスプレッションは固定の制御ティック（推奨 120–240 Hz、既定 192 Hz）で
fnum/TLを書き直す。高いほど滑らかだがVGMが肥大するので、設定可能にして既定値を
計測で決める。

**(f) VGM出力** — VGM 1.71 ヘッダ + コマンド列 + 終端 0x66。gzip しない（中間ファイル）。
総サンプル数はスコアの `durationSeconds` から決め、**リリース余韻のテールを固定長で
足す**（既定 1.0 秒）。他ステムと長さを揃える必要があるので、最終的な尺は
miditrack 側が指定した値に切り詰める。

**(g) レポート** — トラック別の割り当て結果、省略ノート数、音域外ノート数、
スティール回数、警告文（メッセージID）。miditrack がUIに出す。

### 6.3 音色バンク

チップごとのプリセットをJSONデータとして持つ（コードに埋めない）。

```jsonc
{ "id": "opn2:brass-01", "chip": "YM2612", "name": "Brass",
  "gmHints": [56, 57, 61],      // GM音色からの推奨（自動選択に使う）
  "carrierMask": 0b1010,
  "registers": { "B0": 0x3A, "30": 0x71, "40": 0x23, ... } }
```

- **YM2413（OPLL）は内蔵ROM音色15種**をそのまま指定でき、パッチ作成が不要。
  最初に実装するFMチップとして最適。
- **PSG系**（SN76489 / AY-3-8910 / GB DMG / HuC6280）は波形・デューティ・
  簡易エンベロープのみで、自前で少数用意すれば足りる。
- **4op/2op FM**（YM2612 / YM2151 / YM2203 / YM2608 / YM3812）は 24 音色程度を
  自作する。**第三者のパッチバンクをvendorしない** — `CMakeLists.txt` が
  ライセンス未確認のコアを明示的に除外しているのと同じ姿勢を保つ。ユーザーが
  手持ちの `.opm`（VOPM）等を読み込む機能は Phase 3。
- 音色未指定時は `gmHints` と現在のGMプログラム番号から自動選択する。

### 6.4 対応チップ候補

`CMakeLists.txt` で有効なコアのうち、サンプルROM不要で音程演奏に使えるもの。

| チップ | 発音数 | 種別 | Phase | 備考 |
|---|---|---|---|---|
| SN76489 | 3 + noise 1 | 矩形波 | 1 | 音色データほぼ不要 |
| YM2413 (OPLL) | 9（または6+リズム5） | 2op FM | 1 | 内蔵ROM音色15種 |
| AY-3-8910 | 3 | 矩形波 | 2 | |
| YM2612 (OPN2) | 6 | 4op FM | 2 | ch6はDAC（Phase 4） |
| YM2151 (OPM) | 8 | 4op FM | 2 | KC/KFで音程が素直 |
| YM2203 (OPN) | 3 FM + 3 SSG | FM+PSG | 2 | |
| YM2608 (OPNA) | 6 FM + 3 SSG | FM+PSG | 2 | リズム/ADPCMは対象外 |
| YM3812 (OPL2) | 9 | 2op FM | 2 | |
| GB DMG | 2 矩形 + 波形 + noise | 各種 | 2 | 波形メモリの扱いに設計要 |
| HuC6280 | 6 | 波形メモリ | 3 | |

**注意: NES APU は現時点で使えない。** `CMakeLists.txt` は
`SNDEMU_NESAPU_ALL OFF`（MAMEコアはGPL-2.0+、NSFPlay移植はライセンス未確認）で
明示的に除外している。NES音源で鳴らしたい場合は、(a) nsf2midi 側のNotSoFatso由来の
APUに「任意のノート列を鳴らす」入口を新設するか、(b) ライセンス判断を見直すかの
どちらかで、**別の意思決定が必要**。同様に QSound / SAA1099 / WonderSwan も除外中。

---

## 7. miditrack 側の統合

### 7.1 セッション状態

```python
@dataclass(frozen=True)
class ChipVoiceSelection:
    chip_id: str     # "YM2612"
    voice_id: str    # "opn2:brass-01" | None（GMヒントから自動）

# WebSession
track_chips: dict[int, ChipVoiceSelection] = field(default_factory=dict)
```

`clear()` / `reset_midi_state()` で確実に破棄する（既存のライフサイクル契約）。

### 7.2 音源の語彙

`track_sources` に3つ目の値 `"chip"` を足す。UI表記は「チップ」/「Chip」、
`"game"` の「原曲の音源」とは明確に別物として扱う。

`validate_track_sources()`（`web_session.py:255`）にフォーマット非依存の分岐を足す:

- `note_count > 0` かつ単一チャンネルのトラックのみ選択可。
- `chip_id` がカタログに存在し、`voice_id` がそのチップのものであること。
- 選択時は `track_chips` に必ずエントリが要る（未指定ならセッション既定チップ）。

`track_payload()` の `availableSources` に `"chip"` を足し、`chipId`/`voiceId`/
`availableChips` を返す。

### 7.3 レンダリング計画

`_plan_render_jobs()` の `chip_render_indices`（現在は VGM/NSF の `"game"` 限定）を
**「FluidSynthに渡さないトラック集合」**へ一般化する:

```python
hardware_indices = game_indices | chip_synth_indices
```

`_plan_chip_hardware()` と対になる `_plan_chip_synth()` を新設する。既存の
「音量が既定のチャンネルはまとめて1回、変更されたチャンネルだけ個別」という方針は
そのまま適用できる — **同じチップに割り当てられたトラックは1本のVGMにまとめて
1回のエミュレーションで描画**し、音量を個別に変えたトラックだけ別VGMにする。

新モジュール `miditrack/src/miditrack/chip_synth.py` は `libvgm.py` / `nsf_chip.py` と
同じ形にする（Node CLIの解決は `convert.resolve_converter_argv0()` を再利用、
argv配列 + `shell=False`、タイムアウト、stderr末尾付きエラー、`has_wave_audio()` 検証）。
`RuntimeDependencies` に `chip_synth_renderer` を足してテストの注入点を保つ。

### 7.4 キャッシュ（重要な差分）

`"game"` ステムは元ファイル由来なので `midi_revision` とチャンネル集合だけでキャッシュ
できるが、チップ合成ステムは**編集後MIDI由来**なので、ノート内容が変わるたびに
無効化しなければならない。キャッシュキーに含めるもの:

```
version / midiRevision / applied状態のハッシュ（assignments・volumes・speed・transpose）
/ trackIndices / chipId / voiceId / voiceBankVersion / controlRate / sampleCount
```

`RenderService` がLRUと世代を単独所有する契約は変えない。

### 7.5 ミックスとゲイン

`mix.py` の `STEM_GAIN`(0.55) をチップ合成ステムにも適用する。ただし現在の
ヘッドルーム根拠（`DRY 0.80 + STEM 0.55 = 1.35`）は NOISE/DPCM ステム前提で書かれた
コメントなので、ステム種別が3種になる場合の最悪値を再計算してコメントを更新する。
必要なら合成側で出力を正規化して、ゲイン設計を単純に保つ。

### 7.6 速度・ピッチ

`"game"` ステムは元ファイル再生なので `rubberband.py` で後から追従させているが、
チップ合成ステムは**適用済みMIDIから作る**ため rubberband を通さない。これは音質上
明確に有利（タイムストレッチのアーティファクトが乗らない）で、
`_synced_stem()` の対象から外す。

### 7.7 プロジェクト・エクスポート

- `.miditrack` manifest に `trackChips` を追加。読み込み時に未知のチップ/音色IDは
  `soundfont` へ落として警告（既存の loss-tolerant 方針に合わせる）。
- トラック別ZIPのファイル名サフィックスに `_chip` を追加（既存は `_midi` / `_orig`）。
- バリエーション一括生成は速度・移調を変えた適用済みMIDIから再合成する。
  セッションの値を書き換えない既存の引数渡し設計をそのまま使う。

---

## 8. UI/UX

- **音源トグル**: SF / 原曲 / チップ の3状態。Cmd-click の一括適用は既存規約を踏襲。
- **セレクタ**: 「チップ」選択時、GM音色セレクタの位置にチップ名と音色名を出す。
  お気に入り（★）は `favorite_programs.mjs` と同じ仕組みを流用。
- **一括選択**: ヘッダに「チップ音源を一括で選ぶ」。既定の使い方はこちら。
- **警告**: 既存 `conversionWarnings` の表示枠を再利用。
- **i18n**: 日本語をメッセージIDとし、英語カタログを同時に追加（既存契約）。
- **プレビュー**: チップステムは既存の「変換後にバックグラウンドで先行生成しておく」
  経路（原曲ステムと同じ）に乗せる。

---

## 9. 段階的な実装計画

| Phase | 内容 | 目安 |
|---|---|---|
| **0 · 実証** | 固定スコアから手書きでSN76489のVGMを生成し、既存ヘルパでWAVが出ることを確認。レンダリング時間も計測する。**ネイティブ変更ゼロで方式が成立するかの判定**<br>→ `vgm2midi/scripts/spike-chip-synth.js`（`npm run spike:chip-synth`）。詳細は下の「Phase 0 実証コード」 | 0.5日 |
| **1 · MVP** | スコアJSON契約 / 合成コア / SN76489 + YM2413 / `"chip"` 音源 / レンダリング・ミックス・キャッシュ / UIトグルとセレクタ / テスト一式 | 4–6日 |
| **2 · チップ拡充** | YM2612・YM2151・YM2203・YM2608・YM3812・AY-3-8910・GB DMG / 自作FM音色バンク / ピッチベンド・ビブラート・パン | 5–8日 |
| **3 · 周辺機能** | プロジェクト保存/復元 / トラック別書き出し / バリエーション / VGM書き出し / `.opm` 読み込み / お気に入り・設定の永続化 | 3–4日 |
| **4 · 拡張（任意）** | PCM/ADPCM（ユーザー提供サンプル） / HuC6280 / 「原曲チップのまま編集後ノートを鳴らす」モード / NES APU のライセンス再検討 | 未見積 |

Phase 4 の「原曲チップのまま編集後ノートを鳴らす」は、本機能の副次的だが大きな価値。
現在の「原曲の音源」はノート編集を反映できないという制約を、同じ仕組みで解消できる。

---

## 10. テスト戦略

### 10.1 ラウンドトリップを主軸にする

**既存のVGM→MIDIデコーダを合成器のオラクルとして使う。** 合成したVGMを
`VGMParser` + `MidiConverter` に通し、元のスコアと同じノート番号・同じ発音タイミング・
同じ相対音量が復元されることを検証する。チップごとのレジスタ変換を人手で
期待値化する必要がなくなり、既存デコーダの回帰検知にもなる。

### 10.2 その他

- **単体**: 音程変換の往復誤差（セント上限）、音域クリップ、ボイススティールの決定性、
  音量カーブ、VGMヘッダのバイト列。
- **ゴールデン**: 小さなスコア1本からのVGMバイト列固定（決定的出力の担保）。
- **Python**: `tests/test_chip_synth.py`（`test_libvgm.py` と同形）、
  Web API のトラック音源検証、プロジェクトのラウンドトリップ。
- **ブラウザモジュール**: `track_list.test.mjs` / `track_edits.test.mjs` と同形で
  セレクタの状態遷移。
- **受け入れ**: 任意の `.mid` を投入 → 全トラックYM2413 → WAVダウンロードまで通す。
  実コーパス（`testdata/real-corpus/`）はチップ合成には不要（入力が編集後MIDIのため）。

---

## 11. リスクと未決事項

| # | 論点 | 対応方針 |
|---|---|---|
| R1 | 「チップで鳴らす」が「原曲の音」と誤解される。汎用FMパッチは原曲のようには鳴らない | 名称・文言で明確に区別。READMEでも別項目として説明 |
| R2 | 音色バンクのライセンス | 自作のみ同梱。第三者バンクはユーザーが読み込む形にする |
| R3 | レンダリング時間（編集のたびにチップ数ぶんの全尺エミュレーション） | Phase 0 で実測。遅い場合はトラック単位ステムのキャッシュと差分再合成に切り替え |
| R4 | 同時発音数の制約で音が欠ける | 決定的なスティール + 明示的な警告。将来は同一チップの2インスタンス化（VGMは2個目のチップを表現できる） |
| R5 | NES APU が libvgm 側で無効 | 6.4 の通り別途意思決定が必要。MVPの対象外 |
| R6 | 制御レートとVGMサイズのトレードオフ | 既定192Hzで実測し、設定可能にする |
| R7 | ステムのゲイン設計が3種類に増える | 7.5 の通り最悪値を再計算し、必要なら合成側で正規化 |

### 決めておきたいこと

1. **Phase 1 のチップは SN76489 + YM2413 でよいか。** 音色データ作成が不要で、
   PSG系とFM系を1つずつ押さえられる組み合わせを推奨。
2. **`midi2vgm` を vgm2midi パッケージ内に置くか、別パッケージにするか。**
   チップテーブル共有を理由に同一パッケージを推奨。
3. **音色バンクの規模と作者。** Phase 2 で 24 音色を自作する想定。
4. **NES APU を扱うか（R5）。** 扱うなら nsf2midi 側に入口を作る別案件になる。

---

## 12. 既存契約への影響

更新が必要な文書:

- `miditrack/CLAUDE.md` — 「Rendering and audio」にチップ合成ステムの位置づけ
  （編集後MIDI由来・rubberband非対象・キャッシュ条件の違い）を追記。
  `RuntimeDependencies` の一覧に `chip_synth_renderer` を追加。
- `vgm2midi/CLAUDE.md` — スコープを「VGM→MIDI」から「VGM↔MIDI」へ。
  スコアJSON契約と音色バンクの所在を記載。
- `README.md` / `README_ja.md` — 「What You Can Do」と「SF / Original トグル」の項に
  チップ音源を追加（両言語を揃える既存ルール）。

---

## 付録 · Phase 0 実証コード

`vgm2midi/scripts/spike-chip-synth.js`（使い捨てのspike。Phase 1で作り直す前提）。

```bash
cd vgm2midi
npm run spike:chip-synth                       # 既定: 4秒スコアの検証 + 180秒でのコスト計測
node scripts/spike-chip-synth.js --seconds 300 --keep /tmp/spike
```

検証は5段。C/D/E はネイティブヘルパ（macOS universal binary）が要るため、
それ以外の環境では A/B だけを実行して `PARTIAL` で終わる（`--require-render` で失敗扱い）。

| | 内容 | ネイティブ |
|---|---|---|
| A | 固定スコア（4小節・120BPM・トーン3声 + ノイズ）からVGMコマンド列を生成 | 不要 |
| B | 生成VGMを既存の `VGMParser` + `MidiConverter` に通し、元のノートが復元されるか | 不要 |
| C | `vgm2midi_stems` の mix モードと `--selection` で、要求した尺ちょうどの可聴WAVが出るか | 要 |
| D | ch0のみを描画したWAVの実測周波数（ゼロクロス）とスコアのセント差 | 要 |
| E | 実曲の長さ（既定180秒）での描画時間と実時間比 | 要 |

### Linux上で確認できたこと（A/B）

- トーン28ノートが**欠落ゼロ**で往復デコードされた（音高一致・発音時刻±30ms以内）。
- ノイズ20打点が GM 42 / 38 / 45（クローズドハイハット・スネア・ロータム）として復元された。
  `midi-math.ts` の3帯域マッピングどおり。
- この構成のSN76489が鳴らせる最低音は **A2（ノート45）** と算出された。10bitの周期
  レジスタ幅で決まる制約で、ベースパートの音域設計に直接効く。Phase 1では音域外ノートの
  扱い（オクターブ上げ / 省略 / 警告）を決める必要がある。

### mutation testで確かめた検査の有効性

検査が空回りしていないことを、生成側を意図的に壊して確認した。

| 変異 | 結果 |
|---|---|
| 音程変換を半音ずらす | 往復デコードが28ノート全欠落として検知 |
| ノイズのノートオフを削除 | **往復デコードでは素通り** → 構造検査を追加して検知 |
| トーンのノートオフを削除 | 構造検査が検知 |

2つ目は重要な発見で、デコーダはノイズの制御レジスタ書き込みごとに打点を作るため、
消音を丸ごと落としても復元される打点数が変わらない。鳴りっぱなしは耳には一発で分かるが
往復デコードには映らないので、発音と消音の1:1対応を生成側の不変条件として別に検査している。
**Phase 1でラウンドトリップをテストの主軸にする際、この盲点を前提に設計する必要がある。**

### macOSで確認すべきこと（C/D/E）

このコンテナはLinuxで、同梱ヘルパは Mach-O（arm64 + x86_64）のため実行できない。
リポジトリをMacに持っていって同じコマンドを流すと C/D/E が走る。**判定の本体はEの実時間比**で、
ここが遅ければ Phase 1 のキャッシュ設計（トラック単位ステム / 差分再合成）を先に決める。
