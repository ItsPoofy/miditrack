# NESサウンドフォントで「NES版にする」機能 — 立案

既存の曲をNESの音で鳴らす手段として、チップエミュレーションではなく
**NES音色のSoundFontをFluidSynthで鳴らす**案。`CHIP_VOICE_PLAN_ja.md`（チップ合成案）
の代替ではなく、**先にやるべき別の案**として提案する。

---

## 1. 結論

**これを先にやる。** 理由は3つあり、3つ目が決定的。

1. **音色データを作る問題が、さらに消える。** チップ合成案の最大の未知数は「FM音色を
   24個自作する」という主観的な作業だった。NESの音色は、**自由に使ってよいと
   明記されたSoundFontがネット上に既に存在する**（§4.1）。生成すら要らない —
   探して、置くだけ。
2. **レンダリング経路を一切増やさない。** FluidSynthは既に任意の`.sf2`を鳴らしており、
   SPCの「原曲の音源」が既に非GMバンクを使っている。ステム合成もrubberbandも要らない。
3. **チップ合成案では、そもそもNESが今できない。** `native/CMakeLists.txt` が
   libvgmのNES APUコアを明示的に無効化している。つまり「NES版にしたい」という当初の
   目的に対して、チップ合成案は**現時点で答えられない**。

やりたいことが「既存音楽をNES版にする」なら、こちらが本線になる。
（§4で当初案の「自前生成」は撤回し、配布されている実例を使う方針に変更した。）

### 前回の立案からの訂正

`CHIP_VOICE_PLAN_ja.md` のR5で「NES APUを扱うならライセンス判断の見直しが要る」と
書いたが、これは正確ではなかった。**nsf2midi自体が既にGPL-2+で、NotSoFatsoのNES APUを
同梱・出荷している。** 無効化されているのは *libvgmヘルパ側* のNES APUコアで、これは
「libvgmヘルパをGPLにしない」という判断であってプロジェクト全体の方針ではない。
したがって真のNESチップ合成が要るなら、置き場所はlibvgmヘルパではなく
**既にGPLのnsf2midi側**であり、判断すべきはライセンスではなくスコープ。

---

## 2. 何が得られて、何が得られないか

線引きをはっきりさせておく。SoundFontで得られるのは**音色**であって、**制約**ではない。
そしてNESらしさの半分は制約から来る。

| | SoundFont方式 | 備考 |
|---|---|---|
| 矩形波・三角波・ノイズの音色 | ◎ | 波形は数学的に厳密。エミュレーションと同一の波形を出せる |
| ドラム（ノイズ由来） | ◎ | sf2のbank 128ドラムキットとして入れれば既存のch10経路で鳴る |
| 任意のポリフォニー | ◎ | 12和音でも鳴る — ただしこれは「NESらしくない」側面でもある |
| 速度・ピッチ・書き出し | ◎ | MIDIのままなので既存機能が全部そのまま効く |
| 発音数制限（矩形2＋三角1＋ノイズ1） | ✕ | §5の制約変換で別途やる |
| 音量の4bit量子化 | ✕ | 同上 |
| 音程の11bit周期量子化（高音のズレ） | ✕ | 同上 |
| 発音中のデューティ変更・ハードウェアエンベロープ | ✕ | sf2は1ノート1波形。原理的に不可 |
| DPCMサンプル | ✕ | 実サンプルが要る。対象外 |

「発音中のデューティ変更」だけはこの方式では原理的に届かない。NESの曲が持つ
「シュワッ」という音色変化はこれなので、**原曲の再現**を求めるなら最終的にはチップ合成が要る。
一方**既存の曲をNES風に仕立てる**用途では、音色 + 制約でほぼ足りる。

---

## 3. 音色選択がGMでなくなる問題

指摘のとおり、ここが実装の本体。現状はGM 0–127に固く結びついている。

### 3.1 いまの縛り

- `gm.py` が128音色名の唯一のソースで、`/api/instruments` がそれだけを配信する。
- `midi.validate_assignments()` は `0 <= program <= 127` で弾く。
- `apply_assignments()` は**バンクセレクトを書かない**素の `program_change` を出す
  （`midi.py:517-525`）。つまりバンク0固定。
- お気に入り（`favorite_programs.mjs`）はGMプログラム番号をそのままキーにしている。
- アンサンブルプリセットは `preset.programs[roleId]` で **役割 → GMプログラム番号**。

バンクセレクト自体は既に扱いがある — `_is_bank_select()` と
`write_track_subset(strip_bank_select=True)` が、SPCのゲーム音源MIDIからバンク切替を
剥がす方向で実装済み。**書く方向が無いだけ。**

### 3.2 一般化の設計

音色の識別子を「GMプログラム番号」から「**バンク + プログラム**」へ広げる。

```python
@dataclass(frozen=True)
class ProgramSelection:
    bank: int      # 0-16383（CC0=MSB, CC32=LSB）。GMは常に0
    program: int   # 0-127
```

1. **音色一覧をSoundFontから読む。** `.sf2` の `pdta` → `phdr` チャンクは38バイト固定長の
   レコード（20バイト名 + preset word + bank word + …）で、プリセット一覧はここだけ読めば
   取れる。依存の追加は不要で、Pythonで40行ほど。新エンドポイント
   `GET /api/soundfonts/presets?path=…` がパス+mtimeでキャッシュして返す。
   GM SoundFontに対しても同じ経路が使える（そのバンクが本当に持っている音色だけが出る、
   という副次的な改善になる）。
2. **検証**。`validate_assignments()` にバンク範囲と、「選択中のバンクに実在する
   プリセットか」の検査を足す。存在しないプリセットはGMと同じくエラー。
3. **適用**。`apply_assignments()` は bank ≠ 0 のとき CC0 / CC32 を program_change の
   直前に書く。既存の `strip_bank_select` はGM側へ振り分けたトラックから剥がす役として
   そのまま活きる。
4. **UI**。音色セレクタは「そのトラックのバンクが持つプリセット名」を並べる。バンク選択は
   トラック単位ではなくセッション単位の「音色バンク」セレクタを主導線にする
   （チップ合成案の一括選択と同じ理由で、実際の操作は「全部NESにする」が大半）。
5. **お気に入り・プリセット・プロジェクト**。キーを `program` から
   `bank:program`（＋バンクの同一性）へ広げる。プロジェクト読み込み時に当該バンクが
   無ければGMへ落として警告 — 既存のloss-tolerant方針どおり。
6. **打楽器**。ch10トラックは今も編集不可のままでよい。FluidSynthはch10をbank 128で
   引くので、**NESバンクにbank 128のドラムキットを入れておけば既存経路のまま鳴る**。
   ここは追加実装が要らない。

### 3.3 アンサンブルプリセットが効く

既存の役割セレクタ（`TRACK_ROLES` = 主旋律 / 対旋律 / ベース / 伴奏 / 打楽器）が、
NESの4声にほぼそのまま対応する。

| 役割 | NES | NESバンクのプリセット |
|---|---|---|
| 主旋律 | Pulse 1 | 矩形波 25% |
| 対旋律 | Pulse 2 | 矩形波 12.5% |
| ベース | Triangle | 三角波 |
| 伴奏 | Pulse 2（共有） | 矩形波 50% |
| 打楽器 | Noise | ノイズ（bank 128） |

「NESアンサンブル」を組み込みプリセットとして出荷すれば、ユーザーの操作は
**プリセットを選んで各トラックに役割を振るだけ**になる。既存UIの再利用で、新しい概念が要らない。

---

## 4. NESバンクは自前生成せず、配布されている実例を使う

当初案は自前生成（波形は数学的に決まるので主観の余地がない）だったが、
**自前生成は不要という判断に変更する。** ネット上に配布されているNES系SoundFontを
ユーザー自身が入手し、既存のSoundFont検索ディレクトリへ置いて選ぶ —
今すでにGM SoundFontで行っているのと**全く同じ運用**でよい。

プロジェクトには「SoundFontは同梱もダウンロードもしない」という契約がある
（README、`scripts/install.sh` の「must not download, link, or bundle a SoundFont」）。
この運用ならmiditrack自身は一切フェッチもバンドルもしないので、契約に**そのまま**
沿う。自前生成案が抱えていたsf2ライタの実装コスト（§旧R1）とエイリアシング対策
（§旧R2）は、この変更で丸ごと消える。

### 4.1 見つかった実例

検索で実在を確認した3件。このセッションからは `musical-artifacts.com` /
`audiobombs.com` / `famistudio.org` / `nesdev.org` への実際のフェッチがネットワーク
ポリシーでブロックされ、プリセット一覧やバンク構成の実バイトまでは確認できなかった。
検索結果からわかる範囲を記す。

| 名称 | 配布元 | 構成 | ライセンス |
|---|---|---|---|
| Famicom Multichip Soundfont V2G（stgiga） | musical-artifacts.com/artifacts/793 | **GM互換**。bank 0、128スロットをGM楽器名のまま、音色を2A03/VRC6/VRC7/FDS/N163/S5B/MMC5から選んで当てている | 「Use this as you please」 |
| Konami VRC7 Soundfont（Jusu-Tengu） | audiobombs.com/items/1597 | VRC7専用。VRC7はYM2413(OPLL)派生の2opFM×6chで、**固定パッチ集 + カスタマイズ可能なSineパッチ1個**という構成そのものを再現した音色集と見られる。GM互換かは未確認 | 未確認（配布ページで要確認） |
| Konami VRC6 Soundfont（Jusu-Tengu） | audiobombs.com/items/1596 | VRC6専用 | 未確認 |
| NES Soundfont.SF2（Daniel-176） | github.com/Daniel-176/Usefull-Soundfonts | 未確認 | 未確認（リポジトリ側を確認要） |

**重要な発見**: 最も見つけやすかったstgiga版は**GM互換**である。つまり
「NES/拡張音源の音色で、既存のSF音源トグルのままGMプログラム番号で選ぶ」という
使い方は、**Phase A無しで今日から動く**。ユーザーがこのsf2をSoundFont検索
ディレクトリに置き、既存のSoundFontセレクタで選ぶだけでよい。README/READMEに
「NESサウンドフォントの例」として案内を1行足す程度の変更で価値が出せる。

一方、Jusu-Tenguの VRC7/VRC6 専用フォントのような**チップ固有のパッチ集**は、
GM 128スロットに収まらない/収める意味がない構成である可能性が高く、これを
フルに活かすには §3 のバンク一般化（Phase A）が要る。

### 4.2 このリポジトリ側の制約（nsf2midiの対応チップ）

`nsf2midi/src/pitch.h` の `ChannelKind` は Square / Triangle / Noise / Dpcm /
Vrc6Pulse / Vrc6Saw / Fds / S5B / N163 を持つが、**VRC7とMMC5は含まれない**。
`channel_map.cpp` の `UnsupportedChipName()` も VRC7 / MMC5 / EPSM を
「この移植が未対応の拡張チップ」として明示的に警告対象にしている
（NSF自体がこれらを使う場合、nsf2midiは変換時に警告を出す）。

これは「VRC7パッチが選べない」という意味ではない — 本機能は任意のMIDIトラックへ
任意のプリセットを当てるだけなので、VRC7専用フォントのパッチも通常どおり選べる。
影響するのは**自動サジェスト**（元NSFのチャンネル名からプリセットを推測する将来機能）
の範囲だけで、そこは nsf2midi が実際に出力するラベル（SQ1/SQ2/TRI/NOISE/DMC/
VRC6-SQ1/VRC6-SQ2/VRC6-SAW/FDS/N163-CH*/S5B-SQ*）に対応するチップに限られる。

### 4.3 案内の出し方

miditrackが特定のURLへ誘導すると「配布」に近づくため、README/READMEでは
一般名詞での案内に留める（"General MIDI SoundFont" の案内と同じトーン）:

> NESの音色を持つSoundFont（例: 「Famicom」「NES」「Multichip」等で検索して
> 見つかる、GM互換のものであれば追加設定なしで使える）を上記の検索ディレクトリへ
> 置くと、通常のSoundFont同様に選べます。

---

## 5. NES制約変換（任意レイヤー）

音色だけでは「NES風」止まりになる。**らしさの残り半分は制約**なので、MIDI領域の変換として
別に用意する。エミュレーションが要らないぶん安く、`midi.py` の単体テストで全部検証できる。

1. **声部制限** — トラックを 矩形2 / 三角1 / ノイズ1 へ割り当て、溢れたノートは
   決定的な規則で落として警告に出す。チップ合成案のボイスアロケータと同じ問題だが、
   MIDI領域なので圧倒的に簡単。
2. **単音化** — 各チャンネルを単音にする（和音は最高音を残す等、規則は選択式）。
3. **音量の量子化** — ベロシティを16段へ。三角波は音量制御を持たないので固定値にする。
4. **音程の量子化** — 11bitの周期レジスタが表せる音程へ丸める。高音ほどズレが大きくなる、
   実機そのままの副作用が出る。

全部トグルにして、既定はオフ（＝音色だけ）にする。「NESっぽさ」をどこまで効かせるかを
ユーザーが選べるのがこの方式の強みで、エミュレーションでは出せない柔軟さになる。

---

## 6. 段階と工数

自前生成（旧Phase B）が丸ごと不要になったため、2段構成に縮む。

| Phase | 内容 | 目安 |
|---|---|---|
| **0 · すぐ出せる価値** | README/READMEにGM互換NESフォントの案内を1行追加するだけ。コード変更なし | 実質0日 |
| **A · バンク選択の一般化** | `.sf2`プリセット読み出しAPI / `ProgramSelection` / バンクセレクト書き出し / 音色セレクタ / お気に入り・プリセット・プロジェクトの対応 | 3–4日 |
| **C · NES制約変換（任意）** | 声部制限 / 単音化 / 音量・音程の量子化 + トグルUI | 2–3日 |

合計 3–7日（Cを含めるかどうかで変動）。**Aだけでも独立した価値がある** —
「手持ちの任意のSoundFontを、GM音色名ではなくそのバンク自身のプリセット名で
選べる」という汎用機能になり、NESに限らず効く。Phase 0 は今日出せる。

---

## 7. チップ合成案との関係

競合ではなく、**Aが両者の共通基盤**になる。

- チップ合成案も「GMでない音色をトラックへ割り当てるUI」を必要としていた
  （チップ + 音色の2段セレクタ）。Phase A はその土台をそのまま提供する。
- 本案の §5 の制約変換は、チップ合成案のボイスアロケータの設計を先に検証することになる。
- 両者が揃うと、ユーザーから見て3段階になる:

| 選択 | 実体 | 用途 |
|---|---|---|
| NES音色（制約オフ） | 配布SoundFont | 既存の編曲のままNESの音にする |
| NES音色（制約オン） | 配布SoundFont + MIDI変換 | 実機で鳴らせそうな編曲へ寄せる |
| チップ合成 | レジスタ列 + エミュレーション | 実機そのもの（NESはnsf2midi側、他チップはlibvgm側） |

**推奨する順序: 0 → A → C → チップ合成案 Phase 0。** チップ合成案は捨てず、
本案で土台とユーザーの反応を得てから着手する。

---

## 8. リスクと決めたいこと

| # | 論点 | 対応方針 |
|---|---|---|
| R1 | 実例フォントの正確な構成が未確認（egressブロックで未フェッチ） | Phase Aのプリセット読み出しAPIが完成すれば、それ自体で実物を調べる道具になる。実装前に確定させたいなら、ユーザー環境（Mac）で実際にダウンロードして調べてもらう必要がある |
| R2 | 案内するフォントのライセンスが不明瞭 | README等でURLを直接埋め込まず、検索語での案内に留める（§4.3）。特定のURLをmiditrackが名指しで推奨すると実質的な配布判断に近づくため避ける |
| R3 | 「NESの音がしない」と言われる（GM互換フォントは音色だけで制約が無い） | 制約変換（§5）がその答え。Cをどこまでやるかは好み次第 |
| R4 | 音色識別子の一般化が既存機能へ波及 | お気に入り・アンサンブルプリセット・プロジェクトの3箇所。いずれもバンク欠落時はGMへ落とす |
| R5 | VRC7/MMC5パッチは手動選択のみ（nsf2midiが変換時にラベルを出さない） | §4.2の通り、自動サジェスト機能の対象外として明示すれば手動選択自体は問題なく機能する |

### 決めたいこと

1. **この順序でよいか**（0 → A → C を先、チップ合成案は後）。
2. **README案内の書き方**（R2）。検索語での案内で十分か、それとも具体的な
   配布元を名指しすることまで踏み込むか。
3. **制約変換をどこまでやるか**。声部制限だけでも効果は大きい。音程量子化は
   「汚さ」を足す方向なので好みが分かれる。
4. **真のNESチップ合成をnsf2midi側で行うか**。§1の訂正のとおりライセンスではなく
   スコープの問題。やるなら nsf2midi に「ノート列を受け取って描画する」入口を足す
   別案件になる。
